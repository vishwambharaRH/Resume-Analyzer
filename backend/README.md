# Resume-Analyzer

## Overview
FastAPI-based backend for parsing resumes and analyzing job-fit.
Implements modular structure with separate components for upload, parsing, reporting, and testing.

## Setup
```bash
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
